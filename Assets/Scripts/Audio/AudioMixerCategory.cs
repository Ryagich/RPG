namespace GameAudio
{
    /// <summary>
    /// Names are intentionally the same as the exposed groups in RPG Audio Mixer.
    /// </summary>
    public enum AudioMixerCategory
    {
        Master = 0,
        UI = 1,
        Game = 2,
        Music = 3,
    }
}
